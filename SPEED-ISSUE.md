# 블로그 성능 분석

## 현황
- **페이지 로드 시간**: 0.5~1초
- **결론**: 앱 코드는 이미 최적화됨. 남은 지연은 네트워크 경로에서 발생

## 성능 분석 결과

### 시간 분배
- **60~80%**: Cloudflare 무료 요금제 지역 + 터널 지터 (200~600ms)
- **~20%**: 콜드 TLS 연결
- **~15%**: 앱 코드 (이미 최적화됨)

### 측정 결과

| 요청 | saens.kr을 통한 TTFB |
|------|-----|
| Static favicon (DB/렌더링 없음) | 172~809ms (매우 큼) |
| / (인증 + 레이아웃만) | ~290~350ms |
| /blog, /blog/82 (전체 쿼리) | ~325~440ms (따뜻함), 840ms+ (스파이크) |

### 분석 내용

1. **앱 서버는 빠름**: Node 서버는 요청당 ~100~170ms만 소비 (인증 라운드트립 + 쿼리 + SSR)
   - 최근 빌드 적용됨 (15:20 프로세스 재시작 후)
   - 병렬화가 효과 있었음 (서버 시간 감소)

2. **터널 경로의 고질적 문제**: ~170ms 이상의 기본 지연 + 매우 큰 변동성
   - 정적 파일(favicon)도 172~809ms로 4배 변동 → 코드가 아닌 전송 문제

3. **근본 원인**: Cloudflare 무료 요금제는 한국 트래픽을 서울에서 제공하지 않음
   - 요청 경로: 당신 → NRT(도쿄)/HKG(홍콩) → 터널을 통해 PC의 한국으로 → 응답도 같은 경로로 역주행
   - 매 요청마다 한국↔일본 왕복 2회 (120~250ms) + QUIC 터널 지터
   - 불변 해시된 JS 자산도 cf-cache-status: MISS (저트래픽으로 에지 캐시에 거의 없음)

**참고**: Supabase는 빠름 (~56ms 따뜻할 때, 서울 지역) → 주요 병목 아님

## 개선 방안 (영향도 순서)

### 1. 한국(또는 직접 네트워크) 호스팅 [최고 영향도]
- **효과**: TTFB 30~60ms 달성 → 200~600ms 전송 세금 제거 (단일 변경으로 가장 효과 큼)
- **방법**:
  - 저비용 한국 VPS (CF 프록시 없음, DNS 전용 모드만 사용)
  - 또는 서울 지역이 있는 배포 플랫폼

### 2. 에지 캐싱 (터널 유지 시) [2순위]
- **Cloudflare Cache Rule 추가**:
  - 정적 자산 공격적 캐싱
  - 익명 HTML을 짧은 Edge TTL(60초)로 캐싱
- **이점**: 블로그는 읽기 위주, 댓글/좋아요는 1분 지연 수용 가능
- **효과**: 캐시된 응답은 도쿄에서 제공 (터널 우회) → 더블홉을 싱글홉으로 감소

### 3. Cloudflare 유료 + Argo Smart Routing [3순위]
- **장점**: ICN 라우팅 제공
- **단점**: 비용이 저가 VPS보다 큼

### 4. 앱 단계 최적화 (마이너) [4순위]
- **대상**: 요청당 auth.getUser() 네트워크 호출 (~50~100ms)
- **해결책**: Supabase 로컬 JWT 검증으로 전환 (getClaims() with 비대칭 서명 키)
- **주의**: 전송 문제 해결 후에만 의미 있음

## 결론

코드 변경으로는 0.5~1초를 개선할 수 없습니다. 이는 **호스팅 결정** 문제입니다.
- 서버 코드: 이미 최적화 완료
- 병목: 지역 및 네트워크 경로 (호스팅 결정 필요)
